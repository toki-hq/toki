# Vite build output — generated by `npm run build` (CI/Docker + locally).
# We commit only dist/index.html as a placeholder so `cargo build` works
# without Node; the real index.html + hashed assets + copied public/ files
# are produced at build time and embedded via rust-embed.
/dist/*
!/dist/index.html
node_modules/
*.tsbuildinfo
